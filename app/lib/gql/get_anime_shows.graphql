query GetAnimeShows($limit: Int!, $offset: Int!) {
    animes(limit: $limit, offset: $offset) {
        aid
        romajiName
        picname
        year
        airDate
        watchProgress {
          progress
        }
    }
}
